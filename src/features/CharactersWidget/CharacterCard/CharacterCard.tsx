import React from 'react'
import s from './CharacterCard.module.scss'

type PropsType = {}

export const CharacterCard = React.memo(({}: PropsType) => {
	return (
		<div className={s.characterCard}>
			
		</div>
	)
})


